import packageJson from '../../package.json';

// Version de la aplicacion (djo-viewer), no confundir con la version de la DJO (DJOVer).
export const APP_VERSION = packageJson.version;
export const APP_NAME = 'DJO-Viewer';
